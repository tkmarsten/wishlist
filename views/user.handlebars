<h1>Welcome to {{username}}'s Wish List Page</h1>

<div class="row row-cols-1 row-cols-md-2 g-4 mt-4">
  {{#each wishlists}}
    <div class="col">
      <div class="card">
        <div class="card-body">
          <a href="/api/wishlists/{{id}}"><h3>{{name}}</h3></a>
        </div>
      </div>
    </div>
  {{/each}}
</div>